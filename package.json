{
  "name": "forkify",
  "version": "1.0.0",
  "description": "Recipe application",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "del-cli dist && parcel index.html --no-source-maps --open --no-cache",
    "build": "del-cli dist && parcel build index.html --no-source-maps --no-optimize --no-content-hash --no-scope-hoist"
  },
  "author": "Zaitsev Dmitry",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "2.8.3",
    "buffer": "^5.5.0",
    "crypto-browserify": "^3.12.0",
    "del-cli": "^5.0.0",
    "dotenv": "^16.1.4",
    "events": "^3.1.0",
    "os-browserify": "^0.3.0",
    "parcel": "^2.8.3",
    "path-browserify": "^1.0.0",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0"
  },
  "dependencies": {
    "core-js": "^3.30.2",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.13.11"
  }
}
